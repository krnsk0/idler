import { css } from '@emotion/react';

export const styles = {
  debug: css`
    position: absolute;
    bottom: 0;
    display: flex;
    flex-direction: row;
    width: 100px;

    button {
      height: 3em;
      display: inline-block;
      width: auto;
    }
  `,
};
